# Write a C++ program that takes 10 integers as input and stores them in an integer array.
**The program should:**
1. Ask the user to enter a number to search in the array.

2. Display how many times the number occurs in the array.

3. Ask the user to enter a number to replace and the new value.

4. Replace all occurrences of the given number with the new value.